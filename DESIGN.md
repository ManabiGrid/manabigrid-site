# ManabiGrid 展示サイト デザイン契約

- status: active
- audience: human
- updated: 2026-07-18
- scope: repository root

## 1. 誰のための画面か

第一読者は、つまずいた場所から中学数学を学び直す13歳前後の生徒です。第二読者は、教材の状態・作り方・ライセンスを確認する保護者、先生、開発者です。

「子ども向け」は、丸い装飾や幼い語り口に置き換えません。開始地点が明確であること、現在位置が分かること、本文が読みやすいこと、答えへ急いで誘導しないことを親切さと定義します。

## 2. ページの仕事と主要行動

- トップ: 学ぶ人が開始地点を決める。主行動は「診断から始める」、副行動は「8単元から選ぶ」。
- 教科・単元: 教材の順序と量を把握し、最初のレッスンを開く。
- レッスン: 本文を読み、紙やノートで解き、最後に別ページの解答を開く。
- 進捗一覧: 大人が制作状態を確認し、表を検索する。
- 全ページ: 出典、CC BY 4.0、誤り報告先、安全上の注意を失わない。

## 3. 検討した視覚方向

### A. READMEバナーをそのまま展開する

青・水色・マゼンタ、発光、ガラス面、教材図版の組み合わせはプロジェクトの勢いをよく表します。一方、長い教材本文へそのまま持ち込むと、数式・SVG・注意枠と競合します。このため、3×3グリッド記号、青を主に中心だけマゼンタにする視線誘導、「学びのマップ」という発想だけを採用しました。発光、ガラス、ネオン、グラデーション、挿絵の常用は採用していません。

参照: 正本 README.md と docs/assets/banner.png

### B. 静かな読み物スタジオ

細い本文列、白地、濃い文字、抑えたリンク色は教材を読む画面に適しています。ただし、そのままでは一般的な記事サイトに見え、ManabiGrid固有の「戻る場所を見つける」構造が弱くなります。

参考にした設計知見: note系のcontent-firstな読書幅、余白、灰色階調。

### C. 学びの座標帳（採用）

番号、罫線、位置表示で、診断を00、中3数学8単元を01–08、巻末資料をREFとして見せます。カードを並べるのではなく、学習の順番を索引として読む設計です。READMEバナーのグリッド記号と、静かな読み物の本文設計を接続します。

参考にした設計知見:

- 教育サービスに必要な信頼感、平易な導線、大きな見出し。
- 1つの主行動、明確な階層、余白と罫線で区切る編集的な設計。
- 写真・装飾が主役になるギャラリー表現は、教材本文の仕事と合わないため不採用。

## 4. 色

主要パレットは6色です。

- #FFFFFF: 紙、本文背景。
- #F4F7FB: 補助面。検索、開始パネル、補足枠。
- #13203A: 本文と主要見出し。
- #CAD3E0: 罫線。
- #155EEF: 操作、順序、フォーカスの主アクセント。
- #C026D3: READMEバナーから受け継ぐブランド署名。ロゴ中央とヒーローの下線だけに限定。

本文の補助色は#13203Aの不透明度で作ります。操作アクセントは1画面につき青を基本とし、状態ごとの多色化は行いません。白黒印刷では、線種、太さ、番号で階層を保持します。

## 5. 書体

- 本文: 端末内のBIZ UDPGothicを第一候補とするUD系ゴシック。
- ページタイトル: 端末内のHiragino Mincho ProNまたはYu Mincho。教材本文の見出しはゴシックのまま。
- 単元番号、ID、英字ラベル: 端末内の等幅書体。

外部フォントは読み込みません。本文は17px、約44rem以内、行間1.88を基準にします。

## 6. レイアウトと部品

- 最大画面幅: 1160px。
- 本文幅: 44rem。
- 余白: 4 / 8 / 12 / 16 / 24 / 32 / 48 / 64pxを基準。
- 角丸: 操作部品のみ2–4px。カード面の大きな角丸は使わない。
- 影: 使わない。
- アニメーション: 使わない。
- グラデーション: 使わない。

固有部品:

- 3×3ブランドマーク: 8セルを青、中心をマゼンタ。
- 学びの地図: 01–08と細い罫線で単元順を示す。
- レッスン位置: LESSON 01 / 11のように、保存・追跡なしで現在位置を表示。
- 解答導線: 本文末の1か所だけに「自分で考えてから / 答えを見る」を表示。
- 補足枠: 色を増やさず、実線・二重線・破線・点線で種類を区別。
- 進捗: 7状態を工程順に並べ、0件を弱める。大きな表は12区分を既定で閉じ、検索時だけ該当区分を開く。

## 7. レスポンシブ

- 900px以下: トップとレッスンを1列化。固定ヘッダーを解除し、本文を先に読む。
- 680px以下: ロゴを日本語1行にし、BETAと2つの主要ナビを同じ行へ収める。359px以下だけ安全側で折り返す。
- 図と表: ページ全体ではなく、その領域だけを横スクロール可能にする。
- 最小想定幅: 320px。固定幅のカードを置かない。

## 8. アクセシビリティと印刷

- lang=ja、skip link、見出し階層、パンくず、ラベル付き検索を維持。
- キーボードフォーカスはリンク、入力、summary、表スクロール領域で明示。
- 表見出しはscope=col、表領域名は列名と連番で固有化。
- 検索件数は1つのlive regionで通知し、JavaScript無効時も初期件数を正しく表示。
- :::補足は無名asideを量産せず、名前付きrole=noteへ変換。
- 印刷時もフッターの出典とCC BY 4.0を残す。長い表はページ分割し、表見出しを反復する。
- 動きに依存しない。prefers-reduced-motionでもスクロール挙動を安全側にする。

## 9. 境界

- 正本リポジトリは読み取り専用。
- 教材本文の意味・数値は変更しない。
- 外部CDN、外部フォント、解析、追跡、入力フォーム、保存、アカウント機能を追加しない。
- 公開操作は、対象リポジトリ、URL、影響、切り戻し方法を示し、リポジトリ所有者の明示承認がある作業だけで行う。現在のv5タスクでは、その承認を受けて全検査後のGitHub Pages公開までを対象にする。
- デザイン変更はbuild_site.py、static/site.css、static/site.js、static/favicon.svgを正として再生成する。

## 10. Fable展示サイトv3 デザインレビューの採否

方向性「学びの座標帳・編集的タイポグラフィ・抑制されたマゼンタ・子どもをナメない」は維持する。以下の番号は受領したデザインレビュー10件に対応する。

1. **採用** — ヒーロー上部と入口パネルを圧縮した。モバイルでは説明を重複表示せず2択を並列化し、390×844pxと1440×1000pxの双方で先頭単元カードが第一画面へ覗くことを実描画検査の合格条件にした。
2. **採用** — 状態カードを未着手→調査済→ドラフト→QA済→外部レビュー済→人間レビュー済→公開済の工程順へ変更。0件は淡色化し、935行を12区分の`details`へ分割した。検索時だけ該当区分を開く。
3. **採用** — 390pxではブランド名を日本語1行にし、BETAを右隣へ置いた。主要ナビ2件も同一行に残し、英字副題は省略した。
4. **採用** — `——`で主題と副題を分離し、副題を小さい別行へ表示する。区切りダッシュは表示上削除し、行頭禁則を構造で解決した。
5. **採用** — 公式・定義・短い式を静かな枠、薄背景、小ラベルで持ち上げた。長い証明や制作注記まで「式」にしないよう、式の自動判定は短い引用へ限定した。
6. **採用** — モバイルの明朝見出しを700相当、文字色を`#0B1326`へ寄せた。本文ゴシックは維持した。
7. **追加リンクは不採用** — 各練習直後や固定ボタンは、未回答のまま解答へ逃げる選択肢を常時増やし、本文中の視覚ノイズと往復判断を増やす。解答を別ページにする原則を守り、レッスン末の大きな1リンクだけを維持する。解答からはブラウザの「戻る」で同じ位置へ戻れる。
8. **一部採用** — wide SVGに局所スクロール領域と「→ よこにスクロール」を追加した。端フェードはグラデーション禁止・白黒印刷との整合から不採用。「初回だけ」は保存を必要とするため採用せず、ヒントは小さくモバイルだけ常時表示する。
9. **採用** — 診断を「点数は出ない。始める場所がわかる。」の断言＋短い補足へ変更。8単元カードのレッスン数と目安時間は正本のREADME／lesson_mapから生成する。
10. **採用** — レッスンと進捗一覧を実ブラウザのprint mediaでPDF化し、公式枠・補足枠・出典フッターを確認する。結果は§14に記録する。

## 11. Fable展示サイトv3 設計レビューの採否

以下の英字・番号は受領した設計レビュー10件に対応する。

### A. 来歴・鮮度

1. **採用** — 全ページのフッターに生成元コミット短縮SHA、生成日、固定コミットリンク、「展示版・正本はGitHub」を表示する。
2. **採用** — `check_site.py`が正本の現在HEADとビルド時コミットを比較する。一致しない時は再ビルド警告を出し、`--self-test-freshness`で警告経路も検査する。
3. **採用** — 各教材末尾に、そのMarkdownの固定コミットURL、Issue、匿名フォーム、安全上の注意を置く。リンク先は正本README記載のものを使う。

### B. 導線の構造

4. **採用** — 診断パッケージの判定表・単元対応表にある単元名を、正本Markdownを変えずサイト変換時に単元ページへリンクする。実際の判定表は`diagnostic_answers`、設計対応表は`diagnostic_map`にあるため、両ページの表を対象にした。本文全体の同語を乱暴にリンク化しない。
5. **採用** — トップの8単元、順序、レッスン数、目安時間は正本の中3数学READMEと単元データから生成する。診断の30〜45分も正本frontmatterから読む。
6. **採用** — 中2数学「一次関数」の通しページを分割せず、H2ベースの節ナビと現在地ハイライトを追加する。モバイルは折りたたみ、デスクトップは側欄とする。

### C. 機能の射程

7. **採用** — ビルド時にページタイトル＋見出しだけの軽量JSONを作り、教材一覧でNFKC正規化した単純部分一致検索を行う。本文全文、入力履歴、個人別記録は索引化・保存しない。
8. **採用** — インラインSVGへ固有の`title`／短い`desc`、`role="img"`、`aria-labelledby`、`aria-describedby`を付ける。SVG内の長い制作プロンプトは読み上げ名に使わず、Markdownの代替テキストを短い説明にする。検査器で全SVGを確認する。

### D. 将来の公開予約

9. **採用（文書化のみ）** — §13に公開前提条件を予約する。今回、ホスティング設定や公開操作は行わない。
10. **採用** — 数式3箇所を`build-report.json`へファイル・行・式として記録し、`MATH_RENDERING_ISSUE_DRAFT.md`に正本側のIssue候補文面を保存した。Issueは起票しない。

## 12. 静的サイトと動的サイトの判断

**静的サイトを継続する。** 現在必要な機能は、正本の変換、閲覧、検索、前後移動、印刷、来歴表示であり、すべてビルド時HTMLと端末内JavaScriptで完結する。ログイン、個人別進捗、投稿、データベース、サーバーAPIは要求されていない。

動的化は、個人情報・認証・運用監視・障害面を増やす一方、現時点の利用者価値を増やさない。正本更新との連動は動的サイトではなく、将来のGitHub push起点の静的再ビルドで実現する。ユーザーの進捗保存など、サーバー側でなければ成立しない要求が具体化した時だけ再検討する。

## 13. 将来公開の前提条件

公開前に次をすべて満たす。

1. サイト生成物にも正本と同等の内部語彙・ローカル絶対パス検疫をかける。これは秘密情報・個人情報全般の検査とは呼ばない。
2. 公開URLを`site.config.json`の1箇所で管理し、canonical URL、OG、robots、sitemapを生成する。
3. 全ページで「これは展示版。正本はGitHub」を明示し、固定コミット来歴を残す。
4. 公開はリポジトリ所有者の明示承認後にだけ実行する。公開先、URL、対象、切り戻し手順を承認対象に含める。
5. 出力root全体を公開しない。公開allowlistは`public_site.py`を単一正本とし、完成HTML、配信用asset、`.nojekyll`、404、robots、sitemap、ローカル情報を含まないbuild reportだけにする。
6. 正本取得→build→check→生成物検疫→allowlist artifact→deployを1本のCIにし、deployは全ゲート成功に依存させる。失敗時は新artifactを配信せず直前の正常版を残す。

GitHubを初めて見る人向けには`about/index.html`で、リポジトリ、Markdown、commit、Issue、サイトとの関係を説明する。外部リンクは新しいタブで開くことと、GitHubへの投稿は公開情報になることを明示する。

## 14. 実描画・印刷の検証記録

検証日: 2026-07-18。正本コミット: `e39e56db17f116fc7c3ffdd42c4832bd6cdfdb54`。

### 画面

- Codexの可視サイドパネルでlocalhost上の実サイトを操作した。トップ、レッスン、進捗、教材横断検索、診断表、長い一次関数ページを確認した。
- 自動実描画はトップ・wide SVGレッスン・進捗一覧の3ページを390×844pxと1440×1000pxで実施し、**3/3＋3/3、JavaScript例外0、ページ全体の横はみ出し0**。
- トップの先頭単元カード上端は390px表示で823.94px（第一画面内20.06px）、1440px表示で965.78px（同34.22px）。スクロール誘因として覗く。
- wide SVGは390px検査で表示領域358px、内容幅672px、`overflow-x:auto`。ページ全体は広げず、モバイルでヒントを表示する。
- 進捗は935行、12折りたたみ、初期open 0。検索「平方根」では該当2行・2区分だけを開き、解除後は全935行へ戻る。
- 教材横断検索「平方根」は単元1件・本文候補21件を返し、固定ページ内アンカーの存在と遷移を確認した。
- 中2一次関数の節ナビから「L2 変化の割合」へ移動し、モバイル／デスクトップ両ナビの`aria-current="location"`更新を確認した。
- 画像: `review/browser/top-{390x844,1440x1000}.png`、`lesson-wide-svg-{390x844,1440x1000}.png`、`progress-{390x844,1440x1000}.png`。

### 印刷

- 可視サイドパネルではprint mediaを有効化できたがPDF出力APIは`Printing is not available`だったため、PDFだけ同じlocalhost・同じChromeエンジンのheadless印刷で実施した。
- レッスン: A4 **4ページ、823,861バイト**。1ページ目の「式」枠は線・ラベル・本文が分離し、4ページ目の雑談枠、来歴、CC BY 4.0、コミット、誤り報告、安全注意が崩れていない。
- 進捗一覧: A4 **53ページ、3,416,446バイト**。初回検査で閉じた`details`内の935行が欠落する不具合を発見し、`beforeprint`で一時展開、`afterprint`で元の開閉状態へ戻すよう修正した。再出力で先頭集計、`jhs-math-3-square-roots`、末尾の`module-jpn-hs-kanji`、来歴フッターを抽出確認した。
- 表見出しはページごとに反復し、行の途中分断を避ける。画面用検索・ナビ・解答ボタンは印刷しない。
- PDFと先頭／末尾プレビュー: `review/print/lesson.pdf`、`progress.pdf`、`lesson-preview.png`、`lesson-footer-preview.png`、`progress-preview.png`、`progress-footer-preview.png`。

## 15. Fable展示サイトv5レビューの採否

以下の番号は、公開前提・GitHub初心者支援・正本連動を含むv5レビュー9件に対応する。

### A. デザイン・認知負荷

1. **採用** — トップ見出しを「つまずいた場所は、」と「次のスタート地点」「になる。」の文節spanへ分けた。画面幅に応じて文節の境界では折れてよいが、語の途中では折らない。モバイルとデスクトップで同じHTML構造を使う。
2. **採用（数値を事実補正）** — 進捗カード群の前に「いま、このサイトで読める教材」を置き、教材一覧へ1クリックで移動できるようにした。ただし、レビュー文の20件は全レジストリ上の外部レビュー済件数であり、掲載中の教材数ではない。表示は正本から生成した「掲載16パッケージ、うち外部レビュー済12」とし、別リンクでレジストリの外部レビュー済20行を検索する。正式な人間レビュー済・公開済とは混同しない。
3. **採用（フォルダ説明を事実補正）** — README、フォルダ、ZIP、Issue 3手順、個人情報注意、4語のミニ辞典をaboutへ追加した。教材本文は`materials/`、`curriculum/`は進捗・単元一覧・制作資料であり、レビュー文の「教材はcurriculum/」は現行正本と一致しないため修正した。GitHub UI変更で陳腐化しやすいスクリーンショットは使わない。

### B. 公開準備・システム

4. **採用** — `site.config.json`の1箇所でbase URL、公開リポジトリ、正本URL、OG画像を管理する。全273 index対象ページへcanonicalとOGを付け、正本READMEバナーをOG画像としてそのまま利用する。`.nojekyll`、`robots.txt`、273 URLの`sitemap.xml`、SVG favicon、noindexの`404.html`を生成する。GitHub Pagesのproject site配下にあるrobotsはorigin直下ではないため、検索エンジンが必ず参照する保証はしない。
5. **採用** — 正本の`.github/workflows/quarantine.yml`をcommit時点のSHA-256へ固定し、公開allowlist内のパスとUTF-8本文へraw語彙・Unicode正規化検査を再現する。正本workflowが変わった場合は検査器の同期を要求する。生成artifactの検疫であり、サイトリポジトリのGit履歴や汎用secret/PIIを検査したとは表現しない。
6. **採用** — 正本更新検知、手動実行、定期実行、build、check、検疫、allowlist packaging、Pages deployをGitHub Actionsへ実装する。公式Actionはfull commit SHAで固定し、buildとdeployの権限を分ける。deploy jobは全ゲート成功時だけ動き、失敗時は前回の正常な公開版を置き換えない。
7. **採用** — 外部リンク到達性は通常ビルドから分離した明示opt-inの一回検査にする。404/410をhard broken、認証・レート制限・一時ネットワーク失敗をblocked/unknownとして区別し、検査時点・参照件数・一意URL件数・結果をJSONへ残す。恒常監視にはしない。
8. **採用** — `MATH_RENDERING_ISSUE_DRAFT.md`を所有者へ提出するIssue草案として更新し、GitHub上の起票はしない。3箇所のうち`similar-figures/lesson_10.md:28`だけを静的MathMLへ試作した。`mfrac`、読み上げ名、元TeX注記を持てるためサイト側の局所解として成立するが、汎用TeXパーサーへ拡張せず完全一致1件に限定する。残る2件はUnicode表示を維持する。

### C. ダークモード

9. **採用** — 夜間閲覧の負担を減らすため`prefers-color-scheme`へ追従する。切替状態を保存するJSは追加せずOS設定だけを使う。教材SVGは色反転せず白い図版用紙に載せ、判読性を保つ。dark規則はscreen限定とし、印刷は常に白地・黒文字・出典フッターありへ戻す。

## 16. v5の公開構成と切り戻し

- 公開先: `ManabiGrid/manabigrid-site`のGitHub Pages project site。
- 公開URL: `https://manabigrid.github.io/manabigrid-site/`。
- 公開単位: `public_site.py`のallowlistから`package_site.py`が作るartifactだけ。
- 自動更新: 正本の現在HEADと公開中build reportのcommitが違う時に再生成する。手動実行も残す。
- 切り戻し: GitHub Pagesの過去deploymentへ戻すか、正常commitを再実行する。Pagesを無効化すれば配信を停止できる。正本リポジトリはどの手順でも変更しない。
- 外部リンク: 新しいタブ、`noopener noreferrer external`、`referrerpolicy=no-referrer`を維持する。
- 個人データ: ログイン、Cookie、解析、フォーム埋め込み、検索語保存、学習履歴保存を行わない。

## 17. v5の最終検証記録

検証日: 2026-07-18。正本コミット: `7bc7571546b95bfe875a7c1300a4b6e9632b76bb`。

### 生成・リンク・検疫

- Markdownは**247/247件、変換率100%**。生成HTMLは**274ページ**（教材247、トップ1、教材一覧1、about 1、404 1、教科7、単元16）。frontmatterは本文へ出さず、`:::`構文365件とSVG 185件を変換した。
- 内部リンク**5,499件**、切れ**0件**。外部リンク参照は**2,017件**。sitemapは404を除く273 URLで、canonical・OG・robots meta・favicon・`.nojekyll`を検査した。
- 正本HEADと生成コミットは一致し、正本の作業ツリーは空のまま。247正本ファイルのSHA-256と教材内数値を生成物と照合した。
- 公開allowlistは**469ファイル、9,279,544 bytes**。allowlist外の混入0。正本検疫workflowのSHA-256 `b0dabf30bcfadcd1eff318e54361304feb64fb659d2e494a31b6651b8ca9bcb9`と一致し、469ファイルのartifact検疫を通過した。対象は公開候補のパスとUTF-8本文で、Git履歴や秘密情報全般を検査したという意味ではない。
- 外部URL一回検査は一意**257 URL**に実行し、成功256、hard broken 0、blocked/unknown 1。唯一の未確定は`https://github.com/signup`の自動アクセスに対するHTTP 403で、リンク切れとは判定しない。通常の生成・公開ゲートからは分離した。

### 実描画・認知負荷・ダークモード

- localhostをHTTP配信し、トップ、wide SVGレッスン、進捗一覧、about、MathMLレッスン、任意不存在URLの404を、390×844px／1440×1000px、ライト／ダークの**6画面×4条件＝24/24画面**で検査した。JavaScript例外0、ページ全体の横はみ出し0、404のHTTP status 404。
- トップの3文節spanは各1行矩形に収まり、390pxでは先頭単元カード上端が772.80pxで第一画面へ71.20px覗く。aboutの2文節も各1行矩形に収まり、最長文節は328.97px。語中改行を避けつつ左右余白を残した。
- 進捗CTAは初期画面で可視。`status=外部レビュー済`では正本一覧1区分だけを開き、20行だけを表示し、その位置へ移動する。935行の表は356pxの局所領域内で800pxまで横スクロールし、ページ全体は390pxを超えない。
- wide SVGは390pxで表示領域358px、内容672px、`overflow-x:auto`。ダークモードでも図版面は白を保つ。静的MathML 1件は外部ランタイムなしで表示し、残る2件はUnicode表示を維持する。
- Codexの可視サイドパネルでも実サイトを操作し、進捗の状態リンクが42件の部分一致になっていた問題、移動先が画面上端へ来ない問題、長表の局所スクロール、about見出しの1px超過を発見して修正した。修正後に同じ24条件を再実行した。
- `prefers-color-scheme: dark`は画面表示だけに適用する。独立レビューで主ボタンが淡青地＋白文字の1.83:1になっていた不具合を検出し、濃紺文字へ修正して**9.68:1**にした。実computed RGBの4.5:1以上をブラウザ回帰ゲートへ追加した。同じダーク設定下でprint mediaへ切り替えて白地へ戻ることもwide SVGレッスンの4条件すべてで検査し、§14の代表2ページPDFを回帰基準として維持する。
- 更新画像: `review/browser/{top,lesson-wide-svg,progress,about,mathml,not-found}-{390x844,1440x1000}{,-dark}.png`。

### Actionsのローカルdry-run

- `check_workflow.py`でpush／毎日03:17 JSTのschedule／手動実行、正本SHA検出、公式Action 4件のfull SHA固定、最小権限、build成功依存のdeploy、`cancel-in-progress: false`を確認した。定期更新が参照する公開build reportのURLも`site.config.json`の`base_url`から導出し、workflow内へ二重定義しない。
- Actionsと同じ別出力先で`build_site.py --no-check`→`check_site.py`→`package_site.py`を通し、247/247件、274ページ、内部リンク切れ0、469ファイルのartifactを再現した。公開中の正本SHAと一致する定期実行はdeployを省略し、pushと手動実行は必ず再生成する。
- 公開URLと本番workflowの結果は、所有者承認済みの初回GitHub Pages配備後に最終報告で示す。正本リポジトリへは書き込まない。
